package main

import "fmt"

func main(){
   var i int
   var k int
    sum := 0
    var a[M][N]int

      for i := 0; i<M; i++{
        for j := 0; j<N; j++{
             a[i][j] = 1
        }
     }
}
